# ── AI Video Assistant ── requirements.txt ────────────────────────────────────
# Python >= 3.10 recommended

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2024.4.9                  # Download audio from YouTube URLs
pydub>=0.25.1                     # Audio file manipulation / format conversion
ffmpeg-python>=0.2.0              # FFmpeg bindings (FFmpeg binary installed via packages.txt)

# ── Speech-to-Text ──────────────────────────────────────────────────────────
faster-whisper>=1.0.0             # Local Whisper transcription (English)
requests>=2.32.0                  # Used to call the Sarvam AI STT-translate API (Hinglish)

# ── LLM Orchestration — LangChain LCEL + Groq ─────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-groq>=0.1.9             # LangChain wrapper for the Groq API (llama-3.3-70b-versatile)

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
langchain-chroma>=0.1.2           # LangChain <-> Chroma vector store bridge
chromadb>=0.5.0                   # Local vector store
sentence-transformers>=3.0.0      # HuggingFace embedding models
langchain-huggingface>=0.0.3      # LangChain <-> HuggingFace embeddings bridge
huggingface-hub>=0.23.0           # Model downloading from HuggingFace Hub
tiktoken>=0.7.0                   # Token counting for text splitting

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0
watchdog>=4.0.0                   # File-change watcher (improves Streamlit hot-reload)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0              # Load API keys from .env (local dev only)
numpy>=1.26.0
tqdm>=4.66.0                      # Progress bars
